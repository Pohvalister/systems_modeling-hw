clear

% 1
v = [1, 2];
w = [-1; -2];
m = [1:2; -1:-1:-2];

% 2
m0 = zeros(2, 2);
m1 = ones(size(m0));
mr = rand(2, 2);
me = diag(2);

% 3  Variant 8: M=m+mr*me–10
%M_check = m + mr * me;
M = m + mr * me - 10;

%4
'Matrix M has:'
tmp = size(M)
